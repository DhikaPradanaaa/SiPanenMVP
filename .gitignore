# ── Dependency directories ──
node_modules
backend/node_modules

# ── Build output ──
dist
dist-ssr
build

# ── Environment variables (SENSITIVE — jangan pernah di-commit!) ──
.env
.env.local
.env.*.local
.env.production
.env.staging
backend/.env

# ── Database (berisi data pengguna) ──
backend/database.sqlite
backend/*.sqlite
backend/*.db

# ── File upload pengguna ──
backend/uploads/*
!backend/uploads/.gitkeep

# ── Log files ──
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Vite & tooling cache ──
*.local
.vite
.cache

# ── Editor & IDE ──
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── OS generated files ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# ── Netlify ──
.netlify

# ── Coverage & test output ──
coverage
*.lcov
